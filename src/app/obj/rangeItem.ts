export class RangeItem {
    name: String
    max: number
    min: number

    constructor(name:String, max:number, min:number){
        this.name = name
        this.max = max
        this.min = min
    }

}